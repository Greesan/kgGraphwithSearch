# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# You.com API Configuration
YOU_API_KEY=your_you_com_api_key_here

# Model Configuration (optional)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_LLM_MODEL=gpt-4o-mini

# Database Configuration (optional)
DB_PATH=./data/knowledge_graph.db

# Server Configuration (optional)
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Logging Level (optional)
LOG_LEVEL=INFO

# Extension Configuration
# Note: Browser extension backend URL is configured in extension/config.js
# Edit that file if you need to change the backend URL (e.g., different port or remote server)
